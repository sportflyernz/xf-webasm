XForms 1.1 Web assembly glue files.

This directory is primarily based on XSLTforms and replaces the XSLTForms implementation of the MIP, Event, Submission, XPath logic with a pure C++
implementation.
